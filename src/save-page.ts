import { browser } from "webextension-polyfill-ts";

console.log("save-page.js");

// browser.browserAction.onClicked.add.getSelected(null, function(tab) {
//   // null defaults to current window
//   let title: string = tab.title;
//   console.log(title);
// });
